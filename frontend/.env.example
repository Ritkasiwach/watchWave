# URL of the Socket.IO backend.
# - Leave EMPTY/unset when the backend serves this frontend (single-service deploy, same origin).
# - Set it when the frontend is hosted separately (Vercel/Netlify), e.g. https://your-backend.onrender.com
# - In local dev it defaults to http://localhost:3001 when unset.
VITE_BACKEND_URL=
