# Frontend

React + TypeScript + Vite client for the YouTube Watch Party. See the [root README](../README.md) for setup and deployment.

```bash
npm install
npm run dev      # http://localhost:5173 (expects the backend on :3001)
npm run build    # type-check + production build into dist/
npm run lint
```
